{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the walking-skeleton window. Product capabilities are added per feature, never broadened wholesale.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
